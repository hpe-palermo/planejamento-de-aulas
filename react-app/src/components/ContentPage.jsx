import React from "react";
import styled from "styled-components";

const ContentPageComponent = styled.div`
    
`;

function SideBar() {
    return (
        <ContentPageComponent>
            {/*  */}
        </ContentPageComponent>
    )
}

export default SideBar;